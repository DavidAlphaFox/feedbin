<%=
demo = Class.new(Phlex::HTML) do
  def template(&)
    render Settings::ControlGroup.new(class: "mb-14") do |group|
      group.header do
        "Header"
      end
      group.item do
        "item"
      end
    end
  end
end
render demo.new
%>


