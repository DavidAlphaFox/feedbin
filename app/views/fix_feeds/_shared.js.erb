$('#<%= dom_id(@subscription, :fixable) %>').css({opacity: 0});
$('#<%= dom_id(@subscription, :fixable) %>').slideUp(200);
$('[data-behavior~=status_component]').replaceWith('<%= j render FixFeeds::StatusComponent.new(subscriptions: @subscriptions) %>')
